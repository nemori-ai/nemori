# LoCoMo 测试评估环境配置模板
# 复制此文件为 .env 并填入你的实际配置

# =============================================================================
# OpenAI API 配置 (必需)
# =============================================================================
# 你的 OpenAI API 密钥
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI API 基础URL (如果使用代理或自定义端点)
OPENAI_BASE_URL=https://api.openai.com/v1

# 聊天模型配置
CHAT_MODEL=gpt-4o-mini

# =============================================================================
# 嵌入模型配置 (可选，用于向量检索)
# =============================================================================
# 嵌入模型 API 密钥 (如果使用本地模型可以设为 EMPTY)
EMBEDDING_API_KEY=EMPTY

# 嵌入模型 API 基础URL (本地模型示例)
EMBEDDING_BASE_URL=http://localhost:6007/v1

# 嵌入模型名称
EMBEDDING_MODEL=text-embedding-ada-002

# =============================================================================
# 评估配置
# =============================================================================
# 评估版本标识符 (用于区分不同的测试运行)
EVAL_VERSION=episode_semantic_test

# 最大并发数 (控制API调用频率)
MAX_CONCURRENCY=64

# 工作线程数 (用于评估脚本)
WORKERS=10

# =============================================================================
# 可选配置
# =============================================================================
# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 数据目录路径
DATA_DIR=data/locomo

# 结果输出目录
RESULTS_DIR=results/locomo
